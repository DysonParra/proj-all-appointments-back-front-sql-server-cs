@model IEnumerable<Project.Models.Schedule>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DtFrom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DtTo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IntEmployeeId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DtFrom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DtTo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IntEmployeeId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IntId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IntId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IntId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
